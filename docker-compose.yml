version: '3.8'

services:
  franchise-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: franchise-api
    ports:
      - "8081:8081"
    env_file:
      - .env
    networks:
      - franchise-network
    restart: unless-stopped

networks:
  franchise-network:
    driver: bridge